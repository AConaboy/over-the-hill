---
import Layout from "../layouts/Layout.astro";
import PageHero from "../components/PageHero.astro";
import ContentBlockView from "../components/ContentBlockView.astro";
import { getPageContent } from "../lib/content";

const { fields, blocks } = await getPageContent("food");
---

<Layout title="Food | Over the Hill" description="Food and drink information for Our Big Party.">
  <main>
    <PageHero
      eyebrow={fields.hero_eyebrow}
      heading={fields.hero_heading}
      introduction={fields.hero_introduction}
      image="/images/spot-flowers.webp"
    />

    <section class="page-content">
      {blocks.map((block) => <ContentBlockView block={block} />)}

      <div class="page-actions">
        <a class="button" href="/rsvp">Complete your RSVP</a>
        <a class="text-link" href="/activities">Next: Activities →</a>
      </div>
    </section>
  </main>
</Layout>
